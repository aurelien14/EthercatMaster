#include "ethercat_device.h"

/*
EtherCAT_Device_t* ethercat_device_create(DeviceConfig_t* cfg) {
	const DeviceDesc_t* desc = cfg->device_desc;
	EtherCAT_Device_t *dev = desc->create(cfg);
	if (!dev)
		return NULL;
}
*/