#include "device.h"

/* Trouve la description protocolaire compatible */
/*const DeviceProtocolDesc_t*
device_find_protocol_desc(const DeviceDesc_t* desc, ProtocolType_t proto)
{
	if (!desc)
		return NULL;

	for (size_t i = 0; i < desc->protocol_count; i++) {
		if (desc->protocols[i].protocol == proto)
			return &desc->protocols[i];
	}
	return NULL;
}

*/

